name: Build Windows EXE

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 22 (Electron 要求)
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies
        run: npm install   # ← 改成 npm install，兼容我们新加的依赖

      - name: Build Next.js + Electron
        run: npm run electron:build
        env:
          NODE_ENV: production

      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: Perler-Beads-Windows-Portable
          path: dist/*.exe
